import prisma from "../../lib/prisma";

export default async function handle(req, res) {
  // const evts = await prisma.event.findMany({
  //   where: { issue_id: 340502797 },
  // });
  //   console.log(evts);
  // res.json(evts);
}
